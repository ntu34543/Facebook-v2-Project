import { ScrollView } from "react-native";
import { HomeContainer } from "./HomeContainer";

function Home() {

  return (
    <ScrollView>
      <HomeContainer />
    </ScrollView>
  );
}

export default Home;