import { ScrollView } from "react-native";
import { HomeContainer } from "./HomeContainer";

function HomePage() {

  return (
    <ScrollView>
      <HomeContainer />
    </ScrollView>
  );
}

export default Home;