import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const useFirestoreCollection = () => {
  return (
    <View>
      <Text>useFirestoreCollection</Text>
    </View>
  )
}

export default useFirestoreCollection

const styles = StyleSheet.create({})